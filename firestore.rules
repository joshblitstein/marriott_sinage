rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Open rules for hotel internal tool with custom username login
    // (not Firebase Auth). Tighten later if Auth is added.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
