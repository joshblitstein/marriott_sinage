rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Organization logos — open for admin app (custom username login, no Firebase Auth)
    match /logos/{allPaths=**} {
      allow read: if true;
      allow write: if request.resource.size < 5 * 1024 * 1024;
    }
  }
}
